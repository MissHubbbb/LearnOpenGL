#version 330 core
out vec4 FragColor;

in vec2 TexCoords;

uniform sampler2D screenTexture;

void main()
{ 
    //只需要对贴图进行采样
    FragColor = vec4(1.0 - vec3(texture(screenTexture, TexCoords)), 1.0);
}