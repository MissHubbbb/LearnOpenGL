#version 330 core
layout (location = 0) in vec3 aPos;
layout (location = 1) in vec2 aTexCoords;

out vec2 TexCoords;

void main(){
    TexCoords = aTexCoords;
    //因为我们在片段着色器中直接采样颜色缓冲中的颜色，所以不需要在顶点着色器中对顶点坐标进行变换（？）
    gl_Position = vec4(aPos, 1.0);		
}