//简单的着色器，只会进行把顶点变换到光空间的操作
#version 330 core
layout (location = 0) in vec3 position;

uniform mat4 lightSpaceMatrix;
uniform mat4 model;

void main(){
    //gl_Position 顶点的归一化的裁剪空间坐标
    gl_Position = lightSpaceMatrix * model * vec4(position, 1.0f);
}